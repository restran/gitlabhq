- page_title "SSH 密钥"
%h3.page-title
  = page_title
  .pull-right
    = link_to "增加 SSH 密钥", new_profile_key_path, class: "btn btn-new"
%p.light
  在增加 SSH 密钥之前需要先
  = link_to "生成密钥。", help_page_path("ssh", "README")
%hr

= render 'key_table'
