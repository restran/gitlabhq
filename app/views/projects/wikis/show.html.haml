= render 'nav'
%h3.page-title
  = @page.title
  = render 'main_links'
- if @page.historical?
  .warning_message
    这是本页面的旧版本。
    可以查看 #{link_to "最近修改版本", project_wiki_path(@project, @page)} 或浏览 #{link_to "历史版本", history_project_wiki_path(@project, @page)}。

%hr

.wiki-holder
  .wiki
    = preserve do
      = render_wiki_content(@page)

%hr

.wiki-last-edit-by
  最后编辑：#{commit_author_link(@page.commit, avatar: true, size: 16)} #{time_ago_with_tooltip(@page.commit.created_at)}
