- page_title @deploy_key.title, "部署密钥"
.row
  .col-md-4
    .panel.panel-default
      .panel-heading
        部署密钥
      %ul.well-list
        %li
          %span.light 标题：
          %strong= @deploy_key.title
        %li
          %span.light 创建时间
          %strong= @deploy_key.created_at.stamp("2011-12-31")

    .panel.panel-default
      .panel-heading Projects (#{@deploy_key.deploy_keys_projects.count})
      - if @deploy_key.deploy_keys_projects.any?
        %ul.well-list
          - @deploy_key.projects.each do |project|
            %li
              %span
                %strong
                = link_to project.name_with_namespace, [:admin, project.namespace.becomes(Namespace), project]
              .pull-right
                = link_to disable_namespace_project_deploy_key_path(project.namespace, project, @deploy_key), data: { confirm: "确定要继续么？" }, method: :put, class: "btn-xs btn btn-remove", title: '从项目中删除部署密钥' do
                  %i.fa.fa-times.fa-inverse

  .col-md-8
    %p
      %span.light 指纹：
      %strong= @deploy_key.fingerprint
    %pre.well-pre
      = @deploy_key.key
  .pull-right
    = link_to '删除', admin_deploy_key_path(@deploy_key), data: {confirm: '确定要继续么？'}, method: :delete, class: "btn btn-remove delete-key"
